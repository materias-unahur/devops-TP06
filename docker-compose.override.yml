services:
  backend:
    environment:
      APP_ENV: development
    volumes:
      - ./backend/app.py:/app/app.py:ro
    command: ["python3", "-m", "flask", "run", "--host=0.0.0.0", "--port=5000", "--debug"]

  db:
    ports:
      - "5432:5432"
